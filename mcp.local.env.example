# Скопируйте в mcp.local.env (файл не коммитится — см. .gitignore)
# Запускайте postgres из docker-compose перед использованием MCP:
#   cd src/go-microservices && docker compose up -d postgres
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sanatorium?sslmode=disable
